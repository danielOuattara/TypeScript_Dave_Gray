const username = "Dave"; // primitive

// username = "John"; // Incorrect

//----------------------------
const user = {
  name: "John",
  country: "China",
};

user.age = 40; // Correct

console.log("user = ", user);
//----------------------------

const OS_List = ["debian", "FreeBSD"];
OS_List.push("OpenBSD");

console.log("OS_List = ", OS_List);
